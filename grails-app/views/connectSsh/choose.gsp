<jssh:chooseLayout 
	file="index" 
	loadtemplate="${loadtemplate }" 
	port="${port}"
	username="${username}"
	password="${password}"
	hostname="${hostname}"
	input="${input }"
	userCommand="${userCommand}"
	pageid="${pageid }"
	pagetitle="${pagetitle }"
	 />

